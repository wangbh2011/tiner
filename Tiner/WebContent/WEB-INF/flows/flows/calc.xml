<?xml version="1.0" encoding="UTF-8"?>  
<flow xmlns="http://www.springframework.org/schema/webflow"  
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"  
    xsi:schemaLocation="http://www.springframework.org/schema/webflow  
 http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">  
 
     <!-- システムスタートUPサービス -->
    <action-state id="startup">
        <evaluate expression="mainService.execute(null,null)" />
        <transition on="ok" to="viewCart" />
        <transition on="syserr" to="syserr" />
    </action-state>
    
    <view-state id="viewCart" parent="systemBaseFlow#baseview"> 
        <transition on="submit" to="viewOrder">  
        </transition>  
    </view-state>  
    <view-state id="viewOrder" parent="systemBaseFlow#baseview">
        <transition on="confirm" to="orderConfirmed">  
        </transition>  
    </view-state>  
    <view-state id="orderConfirmed" parent="systemBaseFlow#baseview">
        <transition on="returnToIndex" to="returnToIndex">  
        </transition>  
    </view-state>  
    <end-state id="returnToIndex" view="externalRedirect:servletRelative:/index.jsp">  
    </end-state>  
 <!-- 
    <action-state id="main" parent="systemBaseFlow#baseaction">
		<evaluate expression="mngEmptyService.execute( flowRequestContext, false, true, null)" />
		<transition on="ok" to="loginInit" />
	</action-state>
	
	
	
	<view-state id="A902DispItemConfig" parent="systemBaseFlow#baseview">
	</view-state>
	-->  
 
	
</flow>  